import { AuthCardFooter } from "@/components/auth";

export function DocumentPrivacyFooter() {
  return (
    <AuthCardFooter>
      Po wysłaniu dokumentu otrzymasz potwierdzenie, a dane będą widoczne tylko
      dla uprawnionych osób.
    </AuthCardFooter>
  );
}
